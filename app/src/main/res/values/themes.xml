<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlkeWallet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AlkeWallet" parent="Base.Theme.AlkeWallet" />
    <style name="boton_celeste">
        <item name="android:textColor">@color/blanco</item>
        <item name="backgroundTint">@color/azul</item>
        <item name="android:textSize">16sp</item>
        <item name="fontFamily">@font/jua</item>
        <item name="cornerRadius">10dp</item>
        <item name="android:minHeight">50dp</item>
    </style>

    <style name="campo_ingresar">
        <item name="android:minHeight">50dp</item>
        <item name="android:background">@drawable/rectangle</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:fontFamily">@font/jua</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/material_dynamic_neutral_variant70</item>
    </style>

</resources>